services:

  app-shell:
    extends:
      file: docker-compose.yml
      service: app-shell
    volumes:
      - ../../../../renewal_survey_files:/var/www/coldfront_app/renewal_survey_files

  web:
    extends:
      file: docker-compose.yml
      service: web
    volumes:
      - ../../../../renewal_survey_files:/var/www/coldfront_app/renewal_survey_files